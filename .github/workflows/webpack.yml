name: Runner Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on:
      - self-hosted
      - Windows
      - X64

    steps:
      - name: Test
        run: |
          echo Runner is working
          hostname
